# 2026-08-14

## Split BoyoLabs/logs into project-logs + daily-logs, added daily-log SOP

Renamed the old logs repo to project-logs (unchanged practice — on-request, deep single-topic write-ups) and created a new public daily-logs repo for a lighter, always-on running record of the day — captured unprompted throughout the day, reviewed and pushed only when asked. Same sensitive-data scrub rule now covers both repos.

## Finished the 3D-printed business card design

Design finished (0.05in card, 0.025in engraving), already sliced and sitting in the print queue. Purpose is organic in-person networking — hand it out after a good conversation, not marketing.

## Added a "Create Note" feature to the file server

New button creates lightweight HTML notes directly in a folder, no upload needed — in-browser editor with syntax highlighting and a rendered preview, kept cleanly separate from regular uploaded HTML files. Tested end-to-end; tickets opened and closed.

## Automatic system updates + Updates tab on the dashboard

Built a full automatic system-update pipeline: weekly `apt` upgrades (never `dist-upgrade`) chained right after the nightly backup on Sundays, a per-package review/decline mechanism that persists until approved, a pause button for the automatic run (manual runs still work regardless), and a reboot-recommended notice on the dashboard, Updates tab, and glance — deliberately detect-only, never auto-reboots, since this box runs long print jobs and streams. Needed a couple of narrowly-scoped sudo grants for the dashboard, installed manually since Claude can't write to `/etc/sudoers.d` directly. Verified working end-to-end; the first real run is left for Sunday or Boyo's own call.
